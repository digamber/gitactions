name: Print message workflow

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: ./print-message@master
        with:
          message: "Hello, world!"

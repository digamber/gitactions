name: Print message workflow

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: ./print-message@main
        with:
          message: "Hello, world!"
